language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-codespeak' ]; then cd .. && eval "mv $currentfolder generator-codespeak" && cd generator-codespeak; fi

